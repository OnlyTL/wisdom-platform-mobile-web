import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xkjn.app',
  appName: 'wisdom-platform-mobile-web',
  webDir: 'dist'
};

export default config;
